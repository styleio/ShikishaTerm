---
title: SHIKISHA-TERM
description: Run Claude Code, Codex and Gemini side by side — and let them hand work to each other. A portable terminal for orchestrating AI CLIs on Windows.
template: splash
# タイトルがサイト名と同じだと "X | X" になるので明示する
head:
  - tag: title
    content: "SHIKISHA-TERM — Run Claude Code, Codex and Gemini side by side"
hero:
  image:
    file: ../../assets/logo.png
  tagline: Run Claude Code, Codex and Gemini side by side — and let them hand work to each other.
  actions:
    - text: Download for Windows
      link: https://github.com/styleio/ShikishaTerm/releases/latest
      icon: down-arrow
      variant: primary
    - text: How automation works
      link: /automation/
      icon: right-arrow
    - text: GitHub
      link: https://github.com/styleio/ShikishaTerm
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

**A single portable `.exe`. No install, no admin rights, no API keys** — it drives the AI
CLIs you already have logged in.

<video src="/demo.mp4" autoplay loop muted playsinline style="width:100%;border-radius:12px" />

<p style="text-align:center;opacity:.72;font-size:.9em;margin:.7em 0 0">
  Works with Claude Code · Codex · Gemini · Aider · Ollama · DeepSeek · Qwen — or any shell over SSH
</p>

<img src="/desktop.png" alt="SHIKISHA-TERM desktop — a mission-control dashboard of several AI agents, each showing whether it is working, waiting or done" style="width:100%;border-radius:12px;margin-top:1.2em" />

## Put your AIs in a room together

Have **Claude, Codex, Gemini and DeepSeek debate a question**, or make one write code
while the others review it. A judge sums it up, and the whole exchange is rendered as a
chat-style **Result** tab — with a one-click Markdown download of the full record.

It is the same engine underneath: a discussion, a code review, or a browser task all end
as a readable transcript you can keep.

## Check on your AI from your phone

Scan a QR code and you can see what every tab is doing and send instructions — from the
train, from a café, from bed. Over [Tailscale](https://tailscale.com/) only your own devices
can reach it, encrypted, from anywhere.

<img src="/phone.png" alt="SHIKISHA-TERM on a phone — a live dashboard of several agents, each showing whether it is working, waiting or done" style="display:block;margin:1.4em auto 0;width:340px;max-width:100%" />

<CardGrid>
  <Card title="Status you can trust" icon="approve-check">
    Every tab shows whether it is **working**, **done**, or **waiting for you** — read from
    the screen itself, so it works with any CLI rather than one vendor's API.
  </Card>
  <Card title="Agents that hand off work" icon="random">
    "When this finishes, send the result to the review tab." Write it in Lua, or describe it
    in plain language and let an AI you already have installed write it.
  </Card>
  <Card title="Brakes that actually hold" icon="warning">
    A limit on how many times agents may pass work along, an emergency stop, per-tab input
    locks, and a sandbox with no file or network access by default.
  </Card>
  <Card title="A real terminal" icon="laptop">
    SSH, Docker, WSL, jump hosts, key files, port forwarding, session logs, legacy
    encodings, IME input and the mouse. Not a wrapper around one tool.
  </Card>
</CardGrid>

## Why it exists

Running one terminal AI is easy. Running **four** is not.

You end up with a window per agent, no idea which one is waiting for you, and a lot of
copy-pasting between them. SHIKISHA-TERM is a terminal that knows the difference between
working, finished and waiting for a human — and can move work between agents on its own.

It talks to whatever runs in a terminal, so it is not tied to one vendor: Claude Code,
Codex CLI, Gemini CLI, DeepSeek, Ollama, Aider, or a plain shell over SSH.

## Get started

1. Download the zip from [Releases](https://github.com/styleio/ShikishaTerm/releases/latest)
   and unzip it anywhere — a USB stick and a synced folder both work
2. Run `SHIKISHA-TERM.exe`
3. Press `[e]` to open the settings screen in your browser and pick which AI runs where

<img src="/settings.png" alt="SHIKISHA-TERM settings — configure each tab (name, command or SSH host, working folder, automation) in a form, with no JSON editing" style="width:100%;border-radius:12px;margin:.4em 0 1em" />

Nothing is written outside that folder.

:::note[Where it fits]
**Windows only** today — it is built on ConPTY. It **drives CLIs rather than replacing
them**: your subscriptions, logins and settings stay exactly as they are. And it is a
terminal, not an IDE.
:::

---

_Every Windows release is built by public GitHub Actions from this source, with a SHA256
published next to the zip. A free code signing certificate has been applied for from
[SignPath Foundation](https://signpath.org/); while that application is pending the builds are
unsigned, so the first run shows a SmartScreen warning — choose **More info → Run anyway**. See the
[code signing policy](https://github.com/styleio/ShikishaTerm/blob/main/SIGNING.md)._
