.env
# 利用者の持ち物 (設定と資格情報)
/config/
workspaces/*.json
!workspaces/*.example.json
# 持ち出したワークスペース (接続先や作業フォルダがそのまま入っている)
*.stws.json
projects/
logs/


# Added by cargo

/target

# 状態ファイル置き場 (トークンや既読などの利用者ごとの状態)
/data/

# 開発中の実行フォルダ (Dev.cmd が作る。利用者の設定が入る)
run/


_material

# 起動ランチャは exe と同じ場所にだけ置く (ルートに置くと何も起動しない)
Window.cmd

#デバッグ用
/screenshot
Take-Screenshot.ps1

#AI
.claude

# 実用テスト機への配置 (このマシン固有のパスを含むため git 管理外)
Deploy.cmd
# demo/scratch artifacts (not source)
/demo*.mp4
/demo*.gif
/_*.png
/scratch_*
